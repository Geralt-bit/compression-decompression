#include <iostream>
#include <fstream>
int main() {
    std::ifstream inputFile("input.txt");
    std::ofstream compressedFile("compressed.txt");

    // Close files
    inputFile.close();
    compressedFile.close();

    return 0;
}
